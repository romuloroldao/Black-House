# Frontend (Vite) — copie para .env na raiz
# Ou na raiz do projeto: npm run setup:local  (cria .env + server/.env)

VITE_API_URL=http://localhost:3001

# Opcional: prioridade sobre VITE_API_URL no contrato de rotas
# VITE_API_BASE_URL=http://localhost:3001

# Daily Agent no portal aluno (Phase 2). Default ligado se omitido.
# VITE_AGENT_DAILY_ENABLED=true
