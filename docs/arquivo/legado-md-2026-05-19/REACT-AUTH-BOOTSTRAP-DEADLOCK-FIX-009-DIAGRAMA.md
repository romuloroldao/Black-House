# FIX-009: Diagrama de Fluxo

**REACT-AUTH-BOOTSTRAP-DEADLOCK-FIX-009**

---

## 🔄 Fluxo Completo de Bootstrap

```
┌─────────────────────────────────────────────────────────────────┐
│                    APLICAÇÃO INICIA                             │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│              AuthContext useEffect()                            │
│  [REACT-AUTH-BOOTSTRAP-DEADLOCK-FIX-009]                        │
│                                                                 │
│  State Inicial:                                                 │
│  - user: null                                                   │
│  - loading: true                                                │
│  - authInitialized: false ◀── NOVO                             │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
              ┌────────────────┐
              │ Tem token?     │
              └────┬─────┬─────┘
                   │     │
          SIM ◀────┘     └────▶ NÃO
           │                    │
           ▼                    ▼
┌──────────────────────┐ ┌──────────────────────┐
│ apiClient.getUser()  │ │ setAuthInitialized   │
│                      │ │       (true)         │
│ [Restaurar sessão]   │ │                      │
└──────┬───────┬───────┘ │ setLoading(false)    │
       │       │         │                      │
   ✅  │   ❌  │         │ user: null           │
       │       │         └──────┬───────────────┘
       ▼       ▼                │
┌──────────────┐ ┌─────────────┐│
│ Token Válido │ │Token Inválido│
└──────┬───────┘ └──────┬──────┘│
       │                │       │
       ▼                ▼       ▼
┌──────────────────────────────────────┐
│ SEMPRE: setAuthInitialized(true)    │ ◀── GARANTIA CRÍTICA
│ SEMPRE: setLoading(false)           │
│                                      │
│ user: válido | null                  │
└──────────────┬───────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────────┐
│          authInitialized === true                               │
│          ✅ Bootstrap Concluído                                 │
└──────────────────────┬──────────────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
┌────────────────────┐      ┌────────────────────┐
│  BootstrapGuard    │      │  ProtectedRoute    │
│                    │      │                    │
│ if (!authInit)     │      │ if (!authInit)     │
│   → SplashScreen   │      │   → LoadingSpinner │
│                    │      │                    │
│ if (authInit)      │      │ if (authInit)      │
│   → Avaliar        │      │   → Avaliar user   │
│     DataContext    │      │                    │
└────────────────────┘      └────────────────────┘
        │                             │
        ▼                             ▼
┌────────────────────┐      ┌────────────────────┐
│ DataContext OK?    │      │ user != null?      │
└────┬───────┬───────┘      └────┬───────┬───────┘
     │       │                   │       │
   ✅│     ❌│                 ✅│     ❌│
     │       │                   │       │
     ▼       ▼                   ▼       ▼
  [READY] [FAILED]          [Children] [/auth]
```

---

## 🎯 Decisões de Roteamento

### ProtectedRoute - Ordem de Verificação

```
┌─────────────────────────────────────┐
│      REQUEST para rota protegida   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 1. authInitialized === true?       │ ◀── NOVO: Bloqueio antes de decidir
└───────┬─────────────────────────────┘
        │
    NÃO │  SIM
        ▼      ▼
 [Loading] ┌──────────────────────────┐
           │ 2. user !== null?        │
           └───────┬──────────────────┘
                   │
               NÃO │  SIM
                   ▼      ▼
            [/auth]   ┌──────────────────────┐
                      │ 3. role allowed?     │
                      └───────┬──────────────┘
                              │
                          NÃO │  SIM
                              ▼      ▼
                       [Redirect] [Children]
                       por role
```

### BootstrapGuard - Ordem de Verificação

```
┌─────────────────────────────────────┐
│     APP BOOTSTRAP                   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ isAuthRoute?                        │
└───────┬─────────────────────────────┘
        │
    SIM │  NÃO
        ▼      ▼
  [Children] ┌──────────────────────────┐
             │ 1. authInitialized?      │ ◀── NOVO: Aguarda auth primeiro
             └───────┬──────────────────┘
                     │
                 NÃO │  SIM
                     ▼      ▼
              [Splash]  ┌──────────────────────┐
                        │ 2. forceRender?      │
                        └───────┬──────────────┘
                                │
                            SIM │  NÃO
                                ▼      ▼
                          [Children] ┌──────────────────────┐
                          (fallback) │ 3. DataContext state?│
                                     └───────┬──────────────┘
                                             │
                                             ▼
                                      [INIT → Splash]
                                      [RESOLVED → Loader]
                                      [READY → Children]
                                      [FAILED → Error]
```

---

## 📊 Estados e Transições

### AuthContext States

```
╔═══════════════════════════════════════════════════════════════╗
║                    ESTADOS DO AUTHCONTEXT                     ║
╚═══════════════════════════════════════════════════════════════╝

Estado Inicial:
┌─────────────────────────────────────┐
│ loading: true                       │
│ authInitialized: false              │ ◀── NOVO
│ user: null                          │
└──────────────┬──────────────────────┘
               │
               ▼ [useEffect executa]
               
Durante Bootstrap:
┌─────────────────────────────────────┐
│ loading: true                       │
│ authInitialized: false              │ ◀── Ainda false
│ user: null                          │
│ [Fetching...]                       │
└──────────────┬──────────────────────┘
               │
               ▼ [API responde]

Estado Final (Sucesso):
┌─────────────────────────────────────┐
│ loading: false                      │
│ authInitialized: true               │ ◀── Sempre true
│ user: { id, email, role, ... }      │
└─────────────────────────────────────┘

Estado Final (Erro):
┌─────────────────────────────────────┐
│ loading: false                      │
│ authInitialized: true               │ ◀── Sempre true
│ user: null                          │
└─────────────────────────────────────┘

Estado Final (Sem Token):
┌─────────────────────────────────────┐
│ loading: false                      │
│ authInitialized: true               │ ◀── Sempre true
│ user: null                          │
└─────────────────────────────────────┘
```

### Invariante Crítico

```
╔═════════════════════════════════════════════════════╗
║  INVARIANTE: authInitialized === true               ║
║                                                     ║
║  ⇒ user está em estado FINAL:                      ║
║    • user = { válido } (token válido)               ║
║    • user = null (sem token ou token inválido)      ║
║                                                     ║
║  NUNCA:                                             ║
║    • user = undefined                               ║
║    • user em transição                              ║
╚═════════════════════════════════════════════════════╝
```

---

## 🔄 Comparação: Antes vs Depois

### ANTES (FIX-008 + FIX-007)

```
App Init
  │
  ├─> AuthContext init
  │     │
  │     ├─> token? → getUser()
  │     │     [ASYNC - sem sinalização de conclusão]
  │     │
  │     └─> loading=false (quando?)
  │
  ├─> BootstrapGuard
  │     │
  │     └─> Aguarda DataContext.isReady ❌
  │          (Não sabe se auth terminou)
  │
  └─> ProtectedRoute
        │
        └─> if (!user) redirect /auth ❌
             (Pode decidir antes de auth terminar)

PROBLEMA: Race condition entre AuthContext e Guards
RESULTADO: Usuário válido → tratado como não autenticado → SOFT-LOCK
```

### DEPOIS (FIX-009)

```
App Init
  │
  ├─> AuthContext init
  │     │
  │     ├─> token? → getUser()
  │     │     [ASYNC]
  │     │     │
  │     │     └─> setAuthInitialized(true) ✅
  │     │          SEMPRE (sucesso, erro, sem token)
  │     │
  │     └─> loading=false
  │
  ├─> BootstrapGuard
  │     │
  │     ├─> if (!authInitialized) → Splash ✅
  │     │     (Aguarda auth terminar)
  │     │
  │     └─> if (authInitialized) → Avaliar DataContext ✅
  │
  └─> ProtectedRoute
        │
        ├─> if (!authInitialized) → Loading ✅
        │     (Aguarda auth terminar)
        │
        └─> if (authInitialized && !user) → /auth ✅
             (Decisão segura - auth terminou)

SOLUÇÃO: Sinalização explícita de conclusão
RESULTADO: Usuário válido → sempre reconhecido → SEM SOFT-LOCK
```

---

## 📈 Timeline de Execução

### Cenário 1: Token Válido (Sucesso)

```
Tempo  │ AuthContext           │ BootstrapGuard      │ ProtectedRoute
───────┼───────────────────────┼─────────────────────┼──────────────────
0ms    │ init: authInit=false  │ check: !authInit    │ -
       │ loading=true          │ → Splash            │
───────┼───────────────────────┼─────────────────────┼──────────────────
100ms  │ getUser() start       │ Splash visible      │ -
───────┼───────────────────────┼─────────────────────┼──────────────────
1500ms │ getUser() success     │ Splash visible      │ -
       │ setAuthInit(true) ✅  │                     │
       │ loading=false         │                     │
───────┼───────────────────────┼─────────────────────┼──────────────────
1501ms │ authInit=true         │ check: authInit ✅  │ check: authInit ✅
       │ user={...}            │ → DataContext eval  │ user={...}
       │                       │                     │ → Children ✅
───────┼───────────────────────┼─────────────────────┼──────────────────
Total: ~1.5s (antes: 20s timeout)
```

### Cenário 2: Token Inválido

```
Tempo  │ AuthContext           │ BootstrapGuard      │ ProtectedRoute
───────┼───────────────────────┼─────────────────────┼──────────────────
0ms    │ init: authInit=false  │ check: !authInit    │ -
       │ loading=true          │ → Splash            │
───────┼───────────────────────┼─────────────────────┼──────────────────
100ms  │ getUser() start       │ Splash visible      │ -
───────┼───────────────────────┼─────────────────────┼──────────────────
800ms  │ getUser() error 401   │ Splash visible      │ -
       │ setAuthInit(true) ✅  │                     │
       │ user=null             │                     │
       │ loading=false         │                     │
───────┼───────────────────────┼─────────────────────┼──────────────────
801ms  │ authInit=true         │ check: authInit ✅  │ check: authInit ✅
       │ user=null             │ → DataContext eval  │ user=null
       │                       │                     │ → /auth ✅
───────┼───────────────────────┼─────────────────────┼──────────────────
Total: ~0.8s (decisão correta: redirect /auth)
```

### Cenário 3: Sem Token

```
Tempo  │ AuthContext           │ BootstrapGuard      │ ProtectedRoute
───────┼───────────────────────┼─────────────────────┼──────────────────
0ms    │ init: authInit=false  │ check: !authInit    │ -
       │ loading=true          │ → Splash            │
       │ no token              │                     │
───────┼───────────────────────┼─────────────────────┼──────────────────
1ms    │ setAuthInit(true) ✅  │ Splash visible      │ -
       │ user=null             │                     │
       │ loading=false         │                     │
───────┼───────────────────────┼─────────────────────┼──────────────────
2ms    │ authInit=true         │ check: authInit ✅  │ check: authInit ✅
       │ user=null             │ → DataContext eval  │ user=null
       │                       │                     │ → /auth ✅
───────┼───────────────────────┼─────────────────────┼──────────────────
Total: ~2ms (decisão instantânea: redirect /auth)
```

---

## 🎯 Pontos de Decisão Críticos

### 1. AuthContext: Sempre Resolver

```
┌─────────────────────────────────────────────────────┐
│ try {                                               │
│   const user = await getUser();                     │
│   setUser(user);                                    │
│   setAuthInitialized(true); ◀── SUCESSO            │
│ } catch {                                           │
│   setUser(null);                                    │
│   setAuthInitialized(true); ◀── ERRO               │
│ }                                                   │
│                                                     │
│ // Sem token:                                       │
│ setAuthInitialized(true); ◀── SEM TOKEN            │
│                                                     │
│ ✅ GARANTIA: authInitialized SEMPRE true           │
└─────────────────────────────────────────────────────┘
```

### 2. Guards: Aguardar Bootstrap

```
┌─────────────────────────────────────────────────────┐
│ if (!authInitialized) {                             │
│   // Não tomar decisões                            │
│   return <LoadingSpinner />;                        │
│ }                                                   │
│                                                     │
│ // authInitialized === true aqui                   │
│ // user está em estado final (null ou válido)      │
│                                                     │
│ if (!user) {                                        │
│   // Decisão segura: auth terminou, user=null      │
│   return <Navigate to="/auth" />;                  │
│ }                                                   │
│                                                     │
│ ✅ GARANTIA: Nunca decidir com estado parcial      │
└─────────────────────────────────────────────────────┘
```

---

**Criado em**: 2026-01-25
